import { useNavigation } from "@react-navigation/native";

import { Text, VStack } from "native-base";

export function UpdatePointsInterest() {
  const navigation = useNavigation();

  return (
    <VStack>
      <Text>UpdatePointsInterest</Text>
    </VStack>
  );
}
