import { useNavigation } from "@react-navigation/native";

import { Text, VStack } from "native-base";

export function CreatePointsInterest() {
  const navigation = useNavigation();

  return (
    <VStack>
      <Text>CreatePointsInterest</Text>
    </VStack>
  );
}
